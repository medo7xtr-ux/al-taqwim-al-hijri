export type Prayer = { key: string; ar: string; en: string; time: string; icon: string };
const pad = (value: number) => String(value).padStart(2, "0");
export function gregorianToHijri(date = new Date()) { const jd = Math.floor(date.getTime() / 86400000 + 2440587.5); const l = jd - 1948440 + 10632; const n = Math.floor((l - 1) / 10631); const l2 = l - 10631 * n + 354; const j = Math.floor((10985 - l2) / 5316) * Math.floor((50 * l2) / 17719) + Math.floor(l2 / 5670) * Math.floor((43 * l2) / 15238); const l3 = l2 - Math.floor((30 - j) / 15) * Math.floor((17719 * j) / 50) - Math.floor(j / 16) * Math.floor((15238 * j) / 43) + 29; const m = Math.floor((24 * l3) / 709); const d = l3 - Math.floor((709 * m) / 24); const y = 30 * n + j - 30; return { day: d, month: m, year: y }; }
export function hijriLabel(date = new Date(), english = false) { const h = gregorianToHijri(date); const monthsAr = ["", "المحرّم", "صفر", "ربيع الأول", "ربيع الآخر", "جمادى الأولى", "جمادى الآخرة", "رجب", "شعبان", "رمضان", "شوّال", "ذو القعدة", "ذو الحجة"]; const monthsEn = ["", "Muharram", "Safar", "Rabi al-Awwal", "Rabi al-Thani", "Jumada al-Awwal", "Jumada al-Thani", "Rajab", "Sha'ban", "Ramadan", "Shawwal", "Dhu al-Qi'dah", "Dhu al-Hijjah"]; return `${h.day} ${english ? monthsEn[h.month] : monthsAr[h.month]} ${h.year}`; }
export function calculatePrayers(latitude: number, longitude: number, date = new Date(), method: "standard" | "ummAlQura" | "mwl" | "egyptian" = "standard"): Prayer[] { const timezoneOffset = -date.getTimezoneOffset() / 60; const seasonal = Math.sin((date.getMonth() / 12) * Math.PI * 2) * 0.35; const longitudeAdjust = (longitude / 15 - timezoneOffset) * 4; const latitudeAdjust = Math.max(-0.4, Math.min(0.4, Math.abs(latitude) / 180)); const sunrise = 6.15 - seasonal + longitudeAdjust / 60 + latitudeAdjust; const sunset = 18.05 + seasonal + longitudeAdjust / 60 - latitudeAdjust; const methodShift = { standard: { fajr: 0, isha: 0 }, ummAlQura: { fajr: 0.05, isha: -0.1 }, mwl: { fajr: -0.08, isha: 0.08 }, egyptian: { fajr: -0.04, isha: 0.04 } }[method]; const fmt = (hours: number) => { const h = Math.floor((hours + 24) % 24); const m = Math.round(((hours + 24) % 1) * 60) % 60; return `${pad(h)}:${pad(m)}`; }; return [{ key: "fajr", ar: "الفجر", en: "Fajr", time: fmt(sunrise - 1.45 + methodShift.fajr), icon: "wb-twilight" }, { key: "sunrise", ar: "الشروق", en: "Sunrise", time: fmt(sunrise), icon: "wb-sunny" }, { key: "dhuhr", ar: "الظهر", en: "Dhuhr", time: fmt(12.2 + longitudeAdjust / 60), icon: "light-mode" }, { key: "asr", ar: "العصر", en: "Asr", time: fmt(15.45 + longitudeAdjust / 60), icon: "wb-cloudy" }, { key: "maghrib", ar: "المغرب", en: "Maghrib", time: fmt(sunset), icon: "brightness-3" }, { key: "isha", ar: "العشاء", en: "Isha", time: fmt(sunset + 1.35 + methodShift.isha), icon: "nights-stay" }]; }
export function todayGregorianLabel(english = false) { return new Intl.DateTimeFormat(english ? "en-US" : "ar-SA", { weekday: "long", day: "numeric", month: "long", year: "numeric" }).format(new Date()); }
export const defaultPrayers = calculatePrayers(15.3694, 44.1910);
